# Tim's Software Factory — Private Pilot

This private repository is reserved for the governed `tims-factory-pilot-001` feature slice: `deterministic_release_readiness_checklist`.

The repository is controlled by Tim's Software Factory. Planner, Builder, and Inspector are distinct GitHub App identities. `main` is protected; merges require deterministic CI, role/path-boundary verification, and an Inspector approval bound to the exact head commit.

No production data, employer data, personal data, credentials, or unrelated project source belongs in this repository.
