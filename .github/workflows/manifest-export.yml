name: manifest-export

on:
  push:
    branches: [feat/controller-evidence]

permissions:
  contents: write

jobs:
  refresh:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          ref: feat/controller-evidence
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97
        with:
          python-version: "3.12"
      - name: Refresh manifest and self-remove
        shell: bash
        run: |
          set -euo pipefail
          rm .github/workflows/manifest-export.yml
          python scripts/build_manifest.py
          git config user.name "tims-software-factory"
          git config user.email "actions@users.noreply.github.com"
          git add MANIFEST.sha256 .github/workflows/manifest-export.yml
          git diff --cached --check
          git commit -m "chore: bind controller evidence integrity"
          git push origin HEAD:feat/controller-evidence
